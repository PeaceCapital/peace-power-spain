[phases.setup]
nixPkgs = ["python311", "nodejs_20", "nodePackages.npm"]

[phases.install]
cmds = ["pip install -r requirements.txt", "cd energy-dashboard && npm ci"]

[phases.build]
cmds = ["cd energy-dashboard && npm run build"]

[start]
cmd = "python energy_server.py"
